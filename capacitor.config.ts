import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.travelcg.tours',
  appName: 'Travelcg App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
